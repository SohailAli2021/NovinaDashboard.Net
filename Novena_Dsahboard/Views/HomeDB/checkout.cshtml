@{ Layout = "~/Views/Shared/_LayoutWeb.cshtml"; }

@{
    ViewBag.Title = "checkout";
}
@*@TempData["Cart"]
<h2>Checkout</h2>


@if (TempData["Cart"] != null)
{


    <table class="table table-responsive">

        <thead>
            <tr>
                <th>ID</th>
                <th>NAME</th>
                <th>PRICE</th>
                <th>QUANTITY</th>
                <th>BILL</th>
                <th>Remove Items</th>
                <th>ADD Items</th>
                <th>Edit Items Quantity</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in TempData["Cart"] as List<Novena_Dsahboard.Models.Cart>)
            {

                <tr>
                    <td>@item.productid</td>
                    <td>@item.Productname</td>
                    <td>@item.price</td>
                    <td>@item.qty </td>
                    <td>@item.bill</td>
                    <td>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("remove",new { id=item.productid})"> &#10008;</a>

                    </td>
                    <td>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Index",new { id=item.productid})"> &#x2719;</a>

                    </td>
                    <td>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Addtocart",new { id=item.productid})">&#xf044;</a>

                    </td>
                </tr>
            }

        </tbody>

        <tfoot>
            <tr>
                <td colspan="4"></td>
                <td>@TempData["total"]</td>

            </tr>
        </tfoot>

    </table>





}
else
{
    <h2 style="color:red"> Cart is empty....</h2>
    @Html.ActionLink("go Back", "Index", "Home")
}

@if (TempData["Cart"] != null)
{

    using (Html.BeginForm())
    {
        <input type="submit" value="shop now" class="btn btn-primary" />
    }
}*@
